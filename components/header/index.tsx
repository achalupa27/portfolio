import ThemeSection from './theme-section';

const Header = () => {
    return (
        <header className='relative'>
            <ThemeSection />
        </header>
    );
};

export default Header;
